import PremiumToolPage from "@/components/seo/PremiumToolPage";
export const metadata = {
	title: "Free Get HTTP Headers Online - No Signup | 30tools",
	description:
		"Free utility tools: Get HTTP Headers. Convert units, compress files, shorten URLs. Fast, accurate, and easy to use. All processing happens locally in your brows",
	keywords: [
		"get http headers free",
		"get http headers online",
		"low competition get http headers tool",
	],
	robots: {
		index: true,
		follow: true,
		googleBot: {
			index: true,
			follow: true,
			"max-video-preview": -1,
			"max-image-preview": "large",
			"max-snippet": -1,
		},
	},
};

export default function ToolPage() {
	return <PremiumToolPage toolId="get-http-headers" />;
}
